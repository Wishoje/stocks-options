# Batch 10 local review

1. Review Home at 1440, 768, and 390 CSS pixels. The heading and primary action should be visible without horizontal scrolling. Product copy should remain readable at 200% zoom.
2. Confirm the hero, offer, and final actions change correctly for guest, checkout-needed, and active-access states.
3. Confirm Home contains no fixed dollar amounts or internal entitlement identifiers. The trial length should follow the shared server offer, and Pricing should remain the authority for current amounts and terms.
4. Read the visible Home FAQs and inspect the page head. FAQ JSON-LD questions and answers must match the visible list.
5. Use View Source on Home, Features, Pricing, and Contact. Each initial response must contain its route-specific title, description, canonical URL, Open Graph fields, and Twitter card before JavaScript runs.
6. On Features, use the sticky section navigation to reach all four groups. Confirm all 12 view rows are present and no product area from `feature-coverage.md` is missing.
7. Check every data-timing statement. Intraday must be described as stored snapshots with source time and freshness. Supported symbols must be conditional on provider data.
8. Tab through preview tabs, feature links, FAQ summaries, and CTAs. At 390 px, no control or text should be clipped and the feature-row image region should follow its copy.
9. Confirm no legacy screenshot, fake chart, auto-rotating carousel, pulsing metric, or continuous animation appears.
